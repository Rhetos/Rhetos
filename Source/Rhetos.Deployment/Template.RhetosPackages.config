<?xml version="1.0" encoding="utf-8"?>
<packages>

  <!--
    Deploying packages from a folder with NuGet packages or online NuGet gallery:
    =============================================================================
    Ommit the 'version' attribute to get the latest package version. The version attribute will then be automatically set on deployment.
    Add the 'source' attribute to use the specific source for the package instead the sources provided in {SourcesConfigurationFile}.
    
    Examples:
    <package id="Rhetos.CommonConcepts" />
    <package id="Rhetos.SimpleWindowsAuth" version="0.9.2" source = "..\..\Install" />
  -->

  <!--
    Deploying packages from the development source folder (unpacked):
    =================================================================
    Use the 'source' attribute for the package's source folder.
    
    Examples:
    <package id="Rhetos.CommonConcepts" source="..\..\CommonConcepts" />
    <package id="Rhetos.SimpleWindowsAuth" source="..\..\SimpleWindowsAuth" />
  -->
  
  <!--
    Deploying legacy package file in .zip format from a folder:
    ===========================================================
    Set the package id and version to match the filename: id + "_" + version.Replace(".", "_") + ".zip"
    Add the 'source' attribute to use the specific source for the package instead the sources provided in {SourcesConfigurationFile}.
    
    Examples:
    <package id="CommonConcepts" version="0.9.12" source="..\OldPackages" />
  -->

</packages>
